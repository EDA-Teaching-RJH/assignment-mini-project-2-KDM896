#The goal of my project was to make a financial tracker that was secure and could keep a running total across separate uses; with an easy to read transaction list. To make sure that the data was clean and correct I used many validating functions to ensure that everything is recorded correctly. 
In my code, I used object-oriented programming when implementing the main class uning inheritance for two more subclasses. i used super(). __int__ to allow me to re use the string in my transaction class to the income and expense subclass allowing it to be more seamless and avoid repeating code.
at the end of the development i added regualr expression to create a validation layer for the date given. i ensured that the date must be kept in the accapted format to avoid confusion when reading the csv file.
i also added a data persistance layer to ensure that the data is not lost when the program is exited. i chose to use csv so i coule create a table of results catagorising each asspect of a transaction. i used csv.DictWriter and csv.Dict reader to achive a more readable file becasue it allowed for the transactions to be classed to each header from the objects attributes.
one of the challenges i faced was  insuring the maths was correct when calculating the blances changes across mutiple uses compared to the starting balance, i did this in the calculate_balance function. the struggle was differentiating between income and expenses which i solved with the transaction type attributes in the classes. i also checked this logic with a pytest library. in the test i used a seperate list to assert the correct outcome that the function should come to which it was checked against
